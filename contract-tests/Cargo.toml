[package]
name = "tests"
version = "0.1.0"
edition = "2018"

[dependencies]
hex = "0.4.3"
blake2 = "0.9.2"

[dev-dependencies]
casper-types = { version = "1.3.2", features = ["std"] }
casper-engine-test-support = "1.3.2"